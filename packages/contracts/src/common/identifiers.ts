/**
 * Portable identifiers shared by contract families.
 * They remain string aliases because persistence and transport own validation.
 */
export type ExecutionId = string;
export type TenantId = string;
